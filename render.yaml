services:
  - type: web
    name: attendance-app
    env: node
    plan: free
    buildCommand: npm ci
    startCommand: node server.js
    # Render will automatically use the Dockerfile if present, you can omit build/start commands if using Docker.
