# Exclude local data file from Vercel deployment
# Without this, Vercel bundles data.json and always reads stale data (passcode stays 1234)
# With this, all reads/writes go through JSONBlob (the live cloud database)
data.json
attendance_sheet.csv
Start_Attendance_App.bat
Dockerfile
render.yaml
server.js
